1) Проанализировать структуру БД vk, которую мы создали на занятии, и внести предложения по усовершенствованию (если такие идеи есть). Напишите пожалуйста, всё-ли понятно по структуре.

1. Таблица профиля:
photo_id INT UNSIGNED COMMENT "Ссылка на основную фотографию пользователя",
Мне кажется, что INT не подходит. Лучше Varchar
2. 
В таблице friendship 
status_id INT UNSIGNED NOT NULL COMMENT "Ссылка на статус (текущее состояние) отношений",
А в таблице friendship_statuses
name VARCHAR(150) NOT NULL UNIQUE COMMENT "Название статуса",
Думаю, что тип должен быть одинаков и быть Varchar

-- Таблица избранного
CREATE TABLE saved_media (
  user_id INT UNSIGNED NOT NULL COMMENT "Ссылка на пользователя",
  name VARCHAR(255) NOT NULL UNIQUE COMMENT "Название типа",
  filename VARCHAR(255) NOT NULL COMMENT "Путь к файлу",
  likes INT UNSIGNED DEFAULT 0 COMMENT "Колличествово лайков"
)


Я запуталась, когда добавлять идентификатор строки, а когда нет. И зачем он нужен…
id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY COMMENT "Идентификатор строки", 

2) Добавить необходимую таблицу/таблицы для того, чтобы можно было использовать лайки для медиафайлов, постов и пользователей.

Мне кажется, что надо в таблицу media добавить строку кол-во лайков с типом INT, 


likes INT UNSIGNED DEFAULT 0 "Колличествово лайков"
Добавить такую же строчку в saved_media, так как там обнуляются и заново ведутся лайки.
Ну и такую же к таблице profiles и post

--Таблица постов на стене пользователя
CREATE TABLE posts_on_user_wall (
id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY COMMENT "Идентификатор строки",
user_id INT UNSIGNED NOT NULL COMMENT "Ссылка на автора поста",
body TEXT NOT NULL COMMENT "Текст поста",
likes INT UNSIGNED DEFAULT 0 COMMENT "Колличествово лайков"
created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "Время создания строки",  
updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP 
)

Также можно добавить для каждого сообщества посты:

--Таблица постов группы
CREATE TABLE posts_on_user_wall (
user_id INT UNSIGNED NOT NULL COMMENT "Ссылка на автора поста",
name VARCHAR(150) NOT NULL UNIQUE COMMENT "Название группы",
body TEXT NOT NULL COMMENT "Текст поста",
likes INT UNSIGNED DEFAULT 0 COMMENT "Колличествово лайков»,
created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "Время создания строки",  
updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT "Время обновления строки"  
)
Однако, я не придумала как хранить список лайкнувших.

3) Хреново работает filedb..Смогла повторить за действиями на уроке, но не смогла заполнить остальные таблицы. Он их не все видит.